{
  "name": "create-array",
  "version": "0.0.0",
  "license": "ISC",
  "description": "Easier array creation",
  "keywords": [
    "array",
    "create",
    "creation",
    "generate",
    "generation"
  ],
  "files": [
    "index.js"
  ],
  "devDependencies": {
    "browserify": "^13.1.0",
    "chai": "^3.5.0",
    "mocha": "^3.1.0",
    "uglify-js": "^2.4.15"
  },
  "scripts": {
    "browserify": "mkdir -p dist && browserify -s createArray index.js | uglifyjs -c > dist/create-array.js",
    "test": "mocha index.spec.js"
  }
}
